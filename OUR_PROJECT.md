# NexaSpark — project notes

- **Web (primary):** `web/storefront/` — static demo, **NexaSpark** branding, INR, cart in `localStorage`.
- **Mobile (secondary):** `mobile/` — placeholder until a native/hybrid app is added.
- **Removed:** Full AWS Retail Demo Store codebase (`src/`, `aws/`, `workshop/`, …).
- **Docs:** `docs/marketplace-ux-reference.md` — pattern ideas only, not copies of any site.
- **AI roles:** `.cursor/rules/` — code tester + full-stack developer only.
